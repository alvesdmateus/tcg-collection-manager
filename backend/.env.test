# Test Environment Variables
# These are used during test execution

NODE_ENV=test
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRES_IN=7d

# Note: Tests use mock database, so these are not actually used
# But they're defined to prevent errors if code tries to access them
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tcg_collection_test
DB_USER=postgres
DB_PASSWORD=test
